stages:
- deploy
- test

deploy_minio:
  stage: deploy
  image:
    name: bitnami/kubectl:latest
    entrypoint: ['']
  script:
    - echo "deploy_minio"
    - kubectl --insecure-skip-tls-verify=true apply -f applications/minio/namespace.yaml
    #- kubectl --insecure-skip-tls-verify=true create secret generic minio-credentials --from-file=MINIO_ROOT_USER=./applications/minio/username.txt --from-file=MINIO_ROOT_PASSWORD=./applications/minio/password.txt -n minio
    #- kubectl --insecure-skip-tls-verify=true apply -f applications/minio/deployment.yaml
    #- kubectl --insecure-skip-tls-verify=true apply -f applications/minio/service.yaml
    
deploy_crawlab:
  stage: deploy
  script:
    - echo "deploy_crawlab"

deploy_torproxy:
  stage: deploy
  script:
    - echo "deploy_torproxy"
    
deploy_kafka:
  stage: deploy
  needs:
    - deploy_crawlab
  script:
    - echo "deploy_kafka"
    
deploy_oniondownloader:
  stage: deploy
  needs:
    - deploy_torproxy
    - deploy_minio
    - deploy_kafka
  script:
    - echo "deploy_oniondownloader"
    

